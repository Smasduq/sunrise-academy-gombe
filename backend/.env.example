# Database — Supabase pooler (URL-encode special characters in the password, e.g. ! -> %21)
DATABASE_URL=postgresql://postgres.YOUR_PROJECT_REF:YOUR_PASSWORD@aws-1-eu-central-1.pooler.supabase.com:5432/postgres?sslmode=require

# Auth
SECRET_KEY=change-me-to-a-long-random-string
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# CORS (comma-separated origins allowed to call the API directly)
# Include every deployed frontend/admin URL. Example:
# CORS_ORIGINS=https://sunrise-academy-gombe.vercel.app,https://sunrise-academy-gombe-cbyn.vercel.app
CORS_ORIGINS=http://127.0.0.1:3000,http://localhost:3000,http://127.0.0.1:3001,http://localhost:3001

# Hugging Face bucket (images)
# Create a token at https://huggingface.co/settings/tokens (write access)
# Bucket ID format: username/bucket-name (from your bucket page URL)
HF_TOKEN=hf_your_token_here
HF_BUCKET_ID=your-username/your-bucket-name
