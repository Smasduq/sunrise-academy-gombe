# FastAPI backend URL — NOT your Vercel frontend URL.
# Client requests are proxied via /api/* rewrites (no CORS). Server-side auth calls this directly.
API_URL=http://127.0.0.1:8000
AUTH_SECRET=replace-with-a-long-random-secret
AUTH_URL=http://localhost:3001
