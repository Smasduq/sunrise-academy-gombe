# FastAPI backend URL — use BACKEND_API_URL on Vercel (do NOT use API_URL; Vercel may override it).
BACKEND_API_URL=http://127.0.0.1:8000
AUTH_SECRET=replace-with-a-long-random-secret
AUTH_URL=http://localhost:3001
